<nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="{{ path('index') }}">Blog de Jordi</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto py-4 py-lg-0">
                <li class="nav-item {{ app.request.attributes.get('_route') == 'index' ? 'active' : '' }}">
                    <a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ path('index') }}">Home</a>
                </li>
                <li class="nav-item {{ app.request.attributes.get('_route') == 'about' ? 'active' : '' }}">
                    <a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ path('about') }}">Sobre mi</a>
                </li>
                <li class="nav-item {{ app.request.attributes.get('_route') in ['blog','single_post'] ? 'active' : '' }}">
                    <a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ path('blog') }}">Blog</a>
                </li>
                <li class="nav-item {{ app.request.attributes.get('_route') == 'contact' ? 'active' : '' }}">
                    <a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ path('contact') }}">Contacto</a>
                </li>

                {# ---- AQUI VA EL LOGIN/LOGOUT ---- #}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ path('app_logout') }}">Cerrar sesión</a>
                    </li>
                {% else %}
                    <li class="nav-item {{ app.request.attributes.get('_route') == 'app_login' ? 'active' : '' }}">
                        <a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ path('app_login') }}">Loguearse</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
